plugins {
    id 'java'
}

group = 'isel.leirt.mpd'
version = 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    implementation project(':lecture-04-03-queries')
}

test {
    useJUnitPlatform()
}